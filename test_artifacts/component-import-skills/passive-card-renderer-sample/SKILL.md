---
name: passive-card-renderer-sample
description: Test passive command component that renders text or Markdown into a PNG file.
---
# Passive Card Renderer Sample

Use this package to test importing a passive script component.

Suggested command:

```bash
python {entry} --text {input_text} --output {output_file}
```

Expected output:

- `*.png`
