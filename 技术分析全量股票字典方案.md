# 技术分析全量股票字典方案

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` before implementing this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将技术分析业务入口的标的识别收敛到全量股票字典与明确市场参数，避免业务入口和技术分析 skill 脚本各自猜测市场导致 A 股、港股、美股等标的误判。

**Architecture:** 业务入口负责把用户输入解析成标准化标的结构，技术分析 skill 只接收明确参数并执行行情获取、指标计算、报告和图表生成。股票字典只负责名称到标准代码的可信映射，不能解析时直接返回错误并提示用户改用代码。

**Tech Stack:** Python, SQLAlchemy, Tushare, pytest, 当前技术分析 v0.2 skill 的 `scripts/analyze_universal.py`。

---

## 当前问题

技术分析当前存在两层标的识别逻辑：

1. 业务入口 `business/investment/technical_analysis.py` 会解析用户输入，例如 `300502.SZ`、`00700.HK`、`AAPL.US`、中文股票名。
2. 技术分析 skill 的 `scripts/analyze_universal.py` 也会根据字符串规则再次判断资产类型，例如 A 股、港股、美股、黄金、期货。

这会造成职责重复。典型问题是：

- `002265.SZ`、`300502.SZ` 这类 A 股代码如果直接传入 skill，脚本没有明确识别 `.SZ` / `.SH` A 股后缀，可能被错误路由。
- 旧逻辑中裸 6 位 A 股代码在 Tushare fallback 里默认补 `.SH`，导致 `300502` 被请求成 `300502.SH`。
- 港股、美股名称即使能被业务层字典识别，skill 仍然依赖自己的行情接口和字符串判断。
- 中文名展示、股票字典、市场识别、行情获取混在一个流程里，后续排查困难。

## 已确认事实

- 当前业务层通过 `subprocess.run()` 调用 active skill 的 Python 脚本，不是只执行提示词。
- 当前 active skill 路径来自配置 `technical_analysis.skill_path`，实际指向 `investment/skills/technical-analysis/.../技术分析v0.2/scripts/analyze_universal.py`。
- 这个 skill 是正常的“说明文档 + 可执行脚本 + 资源文件”结构。`SKILL.md` 定义能力边界和工作流，`analyze_universal.py` 负责实际数据、图表和报告。
- Tushare A 股日线接口可以用于修复 A 股 SZ/SH 后缀问题。
- Tushare 股票字典适合作为名称识别的唯一可信来源。
- 当前 Tushare 港股基础信息可以刷新；美股基础信息在当前环境曾出现代理超时，不能假设稳定可用。

## 设计原则

1. 股票字典只做可信映射，不做模糊猜测。
2. 字典来源只使用 Tushare，移除 AKShare 字典来源。
3. 中文名匹配不到时直接失败，提示用户使用股票代码。
4. 中文名匹配到多个标的时直接失败，提示用户使用股票代码。
5. 业务入口统一解析市场，skill 脚本不再根据字符串猜市场。
6. skill 后续可能作为插件更新，因此业务层不要依赖 skill 内部实现细节。
7. 缓存功能本阶段不纳入方案，后续另行设计。

## 目标输入规则

| 用户输入 | 字典/解析结果 | 传给 skill 的明确参数 |
|---|---|---|
| `300502.SZ` | A 股深市 | `asset_type=a_share`, `market=SZ`, `data_symbol=300502.SZ`, `symbol=300502` |
| `600519.SH` | A 股沪市 | `asset_type=a_share`, `market=SH`, `data_symbol=600519.SH`, `symbol=600519` |
| `300502` | A 股深市，按代码规则补 `.SZ` | `asset_type=a_share`, `market=SZ`, `data_symbol=300502.SZ`, `symbol=300502` |
| `600519` | A 股沪市，按代码规则补 `.SH` | `asset_type=a_share`, `market=SH`, `data_symbol=600519.SH`, `symbol=600519` |
| `00700.HK` | 港股 | `asset_type=hk_stock`, `market=HK`, `data_symbol=00700.HK`, `symbol=HK00700` |
| `hk00700` | 港股 | `asset_type=hk_stock`, `market=HK`, `data_symbol=00700.HK`, `symbol=HK00700` |
| `AAPL.US` | 美股 | `asset_type=us_stock`, `market=US`, `data_symbol=AAPL.US`, `symbol=AAPL.US` |
| `US:AAPL` | 美股 | `asset_type=us_stock`, `market=US`, `data_symbol=AAPL.US`, `symbol=AAPL.US` |
| `新易盛` | 通过 Tushare A 股字典解析 | 同 `300502.SZ` |
| `腾讯控股` | 通过 Tushare 港股字典解析 | 同 `00700.HK` |
| `苹果` | 只有 Tushare 美股字典存在且唯一时解析 | 同 `AAPL.US` |
| `GC` / `COMEX_GOLD` | 黄金别名 | `asset_type=foreign_futures`, `market=COMEX`, `data_symbol=GC`, `symbol=GC` |

## 股票字典方案

### 数据来源

全量股票字典只使用 Tushare：

- A 股：`pro.stock_basic(exchange="", list_status="L", fields="ts_code,symbol,name,exchange")`
- 港股：`pro.hk_basic(list_status="L")`
- 美股：`pro.us_basic()`

### 标准代码

- A 股统一保存为 `000001.SZ`、`600519.SH`。
- 港股统一保存为 `00700.HK`。
- 美股统一保存为 `AAPL.US`。

### 数据库字段

继续使用现有 `investment_stock_symbols` 表：

- `code`: 标准代码，例如 `300502.SZ`
- `name`: Tushare 返回名称
- `market`: `SZ` / `SH` / `HK` / `US`
- `ts_code`: Tushare 原始代码
- `source`: `tushare_a` / `tushare_hk` / `tushare_us`
- `updated_at`: 刷新时间

### 刷新函数

各市场必须使用明确函数名，不能再使用 `auto` 或泛化 source：

- `refresh_a_share_symbols_from_tushare()`
- `refresh_hk_symbols_from_tushare()`
- `refresh_us_symbols_from_tushare()`
- `refresh_all_symbols_from_tushare()`

`refresh_all_symbols_from_tushare()` 只负责依次调用三个明确函数，并分别记录每个市场成功数量或错误信息。某个市场失败时，不阻断其他市场刷新。

### 中文名解析规则

- 只查 `source in ("tushare_a", "tushare_hk", "tushare_us")` 的数据。
- 精确匹配股票名称。
- 匹配 0 条：返回 `STOCK_NOT_FOUND`。
- 匹配 1 条：返回标准代码。
- 匹配多条：返回 `STOCK_AMBIGUOUS`。
- 不做拼音、简称、别名、模糊搜索。

## 技术分析入口方案

新增一个业务层结构承接解析结果：

```python
@dataclass(frozen=True)
class TechnicalAnalysisTarget:
    normalized_target: str
    skill_symbol: str
    asset_type: str
    market: str
    data_symbol: str
    stock_name: str = ""
```

业务入口职责：

1. 从用户输入提取 `target_text`。
2. 如果是中文，先调用股票字典精确解析。
3. 如果是代码，按市场规则标准化。
4. 生成 `TechnicalAnalysisTarget`。
5. 调用 skill 时传入明确参数。
6. 如果无法解析，返回面向用户的错误提示，不进入 skill。

建议调用参数：

```bash
python analyze_universal.py \
  --symbol 300502 \
  --asset-type a_share \
  --market SZ \
  --data-symbol 300502.SZ \
  --days 120 \
  --output <output_dir>
```

## Skill 脚本改造方案

当前先搁置实现，但后续要完成“skill 不再判断股票市场”，脚本需要支持显式参数：

```python
parser.add_argument("--asset-type", default="", help="业务入口解析后的资产类型")
parser.add_argument("--market", default="", help="业务入口解析后的市场")
parser.add_argument("--data-symbol", default="", help="行情接口使用的标准代码")
```

脚本行为：

1. 如果 `--asset-type` 和 `--data-symbol` 存在，优先使用显式参数构造配置。
2. 显式参数存在时，不再进入 `build_dynamic_config()` 的字符串猜测分支。
3. A 股使用 `data_symbol` 调 Tushare，例如 `300502.SZ`。
4. 港股和美股先继续沿用 v0.2 当前行情接口；如果稳定性不足，再增加对应市场的 Tushare 或其他数据源 fallback。
5. 未传显式参数时保留 v0.2 原有行为，保证插件单独运行不破坏。

## 分阶段实施计划

### Task 1: 固化股票字典刷新与解析测试

**Files:**

- Modify: `tests/test_investment_business.py`
- Modify: `business/investment/stock_resolver.py`
- Modify: `scripts/refresh_investment_stocks.py`
- Modify: `channel/web/web_channel.py`

- [ ] **Step 1: 写失败测试，覆盖 A 股、港股、美股刷新函数名**

```bash
py -m pytest tests/test_investment_business.py -k "stock_resolver or refresh_investment_stocks" -q
```

Expected before implementation: tests fail because refresh source is not fully explicit or market function behavior is incomplete.

- [ ] **Step 2: 实现明确市场刷新函数**

Ensure these functions exist and are exported:

```python
refresh_a_share_symbols_from_tushare
refresh_hk_symbols_from_tushare
refresh_us_symbols_from_tushare
refresh_all_symbols_from_tushare
```

- [ ] **Step 3: 移除 AKShare 字典来源**

Delete `refresh_from_akshare`, remove web/CLI `akshare` source choices, and ignore legacy `akshare` rows during name resolution.

- [ ] **Step 4: 跑测试**

```bash
py -m pytest tests/test_investment_business.py -k "stock_resolver or stock_refresh or refresh_investment_stocks" -q
```

Expected: all selected tests pass.

### Task 2: 将业务入口解析成明确标的结构

**Files:**

- Modify: `tests/test_investment_business.py`
- Modify: `business/investment/technical_analysis.py`

- [ ] **Step 1: 写失败测试，覆盖代码解析**

Cases:

- `300502.SZ` -> `asset_type=a_share`, `market=SZ`, `data_symbol=300502.SZ`, `skill_symbol=300502`
- `600519` -> `asset_type=a_share`, `market=SH`, `data_symbol=600519.SH`, `skill_symbol=600519`
- `00700.HK` -> `asset_type=hk_stock`, `market=HK`, `data_symbol=00700.HK`, `skill_symbol=HK00700`
- `AAPL.US` -> `asset_type=us_stock`, `market=US`, `data_symbol=AAPL.US`, `skill_symbol=AAPL.US`

- [ ] **Step 2: 写失败测试，覆盖中文名解析**

Cases:

- `新易盛` with Tushare row -> `300502.SZ`
- `腾讯控股` with Tushare row -> `00700.HK`
- `苹果` without Tushare US row -> `STOCK_NOT_FOUND`
- duplicate Chinese name -> `STOCK_AMBIGUOUS`

- [ ] **Step 3: 实现 `TechnicalAnalysisTarget` 扩展字段**

Add:

```python
asset_type: str
market: str
data_symbol: str
```

- [ ] **Step 4: 业务入口无法解析时不调用 skill**

If Chinese name cannot be resolved or is ambiguous, return user prompt immediately.

- [ ] **Step 5: 跑测试**

```bash
py -m pytest tests/test_investment_business.py -k "technical_analysis_resolves_tushare or technical_analysis_name_miss or technical_analysis_non_a_share" -q
```

Expected: all selected tests pass.

### Task 3: 为 skill 脚本增加显式参数兼容层

**Files:**

- Create or Modify: `tests/test_technical_analysis_skill_v02.py`
- Modify: `investment/skills/technical-analysis/<active-version>/技术分析v0.2/scripts/analyze_universal.py`

- [ ] **Step 1: 写失败测试，验证显式参数跳过字符串猜测**

Test case:

```bash
py -m pytest tests/test_technical_analysis_skill_v02.py -q
```

Expected before implementation: fails because parser does not accept `--asset-type`, `--market`, `--data-symbol`.

- [ ] **Step 2: 在 CLI parser 增加参数**

Add:

```python
parser.add_argument("--asset-type", default="", help="业务入口解析后的资产类型")
parser.add_argument("--market", default="", help="业务入口解析后的市场")
parser.add_argument("--data-symbol", default="", help="行情接口使用的标准代码")
```

- [ ] **Step 3: 显式参数存在时构造配置**

Implement helper:

```python
def build_explicit_config(symbol_code, asset_type, market, data_symbol):
    ...
```

Behavior:

- `a_share` -> `stock_zh_a_hist`, `symbol` uses bare 6 digits, Tushare fallback uses `data_symbol`
- `hk_stock` -> current `stock_hk_hist` behavior
- `us_stock` -> current `stock_us_hist` behavior
- `foreign_futures` -> current `GC` / foreign futures behavior

- [ ] **Step 4: 保留旧行为**

When explicit args are empty, keep existing `build_dynamic_config(symbol_code)` path.

- [ ] **Step 5: 跑 skill 单测**

```bash
py -m pytest tests/test_technical_analysis_skill_v02.py -q
```

Expected: pass.

### Task 4: 业务入口调用 skill 时传明确参数

**Files:**

- Modify: `tests/test_investment_business.py`
- Modify: `business/investment/technical_analysis.py`

- [ ] **Step 1: 写失败测试，断言 subprocess 参数**

Verify command includes:

```text
--symbol 300502 --asset-type a_share --market SZ --data-symbol 300502.SZ
```

- [ ] **Step 2: 修改 `_run_skill()` 参数签名**

From:

```python
def _run_skill(symbol: str, output_dir: Path) -> tuple[Path, Path]:
```

To:

```python
def _run_skill(target: TechnicalAnalysisTarget, output_dir: Path) -> tuple[Path, Path]:
```

- [ ] **Step 3: 调用 skill 时传完整 target**

The command should include explicit args when target fields are present.

- [ ] **Step 4: 跑业务单测**

```bash
py -m pytest tests/test_investment_business.py -k "technical_analysis_uses_skill_cli or technical_analysis_resolves_tushare" -q
```

Expected: pass.

### Task 5: 真实链路验证

**Files:**

- No production file changes expected.

- [ ] **Step 1: 刷新 A 股字典**

```bash
py scripts/refresh_investment_stocks.py --source a_share --json
```

Expected: `success: true`, count greater than 0.

- [ ] **Step 2: 刷新港股字典**

```bash
py scripts/refresh_investment_stocks.py --source hk --json
```

Expected: `success: true`, count greater than 0.

- [ ] **Step 3: 刷新美股字典**

```bash
py scripts/refresh_investment_stocks.py --source us --json
```

Expected when network and Tushare endpoint are available: `success: true`, count greater than 0. If proxy timeout occurs, record exact timeout and keep A 股、港股验证独立通过。

- [ ] **Step 4: 验证 A 股技术分析**

Run business入口 or script equivalent for:

```text
新易盛 技术分析
300502.SZ 技术分析
600519 技术分析
```

Expected: each returns report path and main chart path.

- [ ] **Step 5: 验证港股与美股入口识别**

Run:

```text
腾讯控股 技术分析
00700.HK 技术分析
AAPL.US 技术分析
```

Expected: business入口解析正确。If skill 行情数据源失败, failure detail must point to data source, not symbol parsing.

## 验收标准

1. 技术分析业务入口不再把 `002265.SZ`、`300502.SZ` 误传成美股。
2. 裸 A 股代码 `300502` 被解析为 `300502.SZ`，`600519` 被解析为 `600519.SH`。
3. 中文名只依赖 Tushare 字典；匹配不到或歧义时不会进入 skill。
4. 全量股票字典刷新入口只有 `all`、`a_share`、`hk`、`us`。
5. `akshare` 不再作为股票字典来源。
6. skill 在收到显式市场参数时不再执行字符串猜测。
7. 未传显式参数时，skill v0.2 仍可独立运行原有输入规则。

## 暂缓事项

- 中文名显示到图片主标题。
- 技术分析缓存策略。
- 股票简称、别名、拼音、模糊搜索。
- 美股行情数据源 fallback 完整改造。
- 港股行情数据源 fallback 完整改造。

