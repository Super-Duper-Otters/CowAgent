# 用户管理页 UI 重构计划

日期：2026-06-01

## 范围

只重构后台 Web 的“投资业务 / 用户管理”页面内部 UI，不调整其他后台页面、不新增复杂 API。

## 目标

- 客户、后台人员仍使用两个子页面 tab。
- 子页面改为列表优先：顶部工具栏负责搜索、新增、导入、导出；主区域展示表格。
- 新增和编辑不再常驻占用页面空间，改为弹层表单打开。
- 后台人员子页面字段保持精简，只保留账号、密码、角色、启用状态。
- 保持现有权限控制、接口路径和业务逻辑不变。

## 具体任务

1. 客户子页面
   - 移除常驻“用户维护 / Excel 导入 / Excel 导出”三块面板。
   - 新增顶部工具栏：关键字搜索、新增客户、导入文件、导入、导出状态、导出。
   - 客户列表 API 支持 `keyword`、`page`、`page_size`，返回 `pagination`。
   - 新增/编辑客户继续使用 `openInvestmentUserDialog` 弹层表单。

2. 后台人员子页面
   - 移除常驻“后台人员维护”表单。
   - 新增顶部工具栏：账号/角色搜索、新增后台人员。
   - 后台人员列表 API 支持 `keyword`、`page`、`page_size`，返回 `pagination`。
   - 新增/编辑后台人员使用新的 `openInvestmentAdminUserDialog` 弹层表单。
   - 行操作保留编辑、启用/停用、重置密码。

3. 样式
   - 增加用户管理页专用容器和工具栏样式。
   - 工具栏按搜索/新增和导入/导出分组，避免按钮、输入框、文件选择控件散乱。
   - 保持现有配色、按钮、表格、弹层风格一致。
   - 移动端工具栏允许换行，避免按钮和输入框挤压。

4. 测试
   - 增加静态 UI 测试，验证用户管理页不再渲染常驻表单。
   - 验证客户/后台人员新增入口都打开弹层。
   - 继续跑 `tests/test_investment_web_ui.py`，确保其他后台静态 UI 不回退。
